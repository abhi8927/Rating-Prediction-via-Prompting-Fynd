# Environment Setup

Create a `.env` file in the `task2/backend/` directory with the following content:

```
GEMINI_API_KEY=AIzaSyDnfybUacyg2A4WqPR7GjhuLVY00r18xh4
PORT=5000
```

The `.env` file is already in `.gitignore` so it won't be committed to the repository.
